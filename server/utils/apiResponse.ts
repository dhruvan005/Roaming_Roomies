// import { Hono } from "hono";

export const apiResponse = {

//     success: (c, data, status = 200, message = "Success") => {
//         return c.json({
//             success: true,
//             message,
//             data
//         }, status);
//     },

//     error: (c, message = "An error occurred", status = 400, errors = null) => {
//         const response = {
//             success: false,
//             message
//         };
//         if (errors) {
//             response.errors = errors;
//         }
//         return c.json(response, status);
//     },

//     notFound: (c, message = "Resource not found") => {
//         return apiResponse.error(c, message, 404);
//     },

//     serverError: (c, message = "Internal server error") => {
//         return apiResponse.error(c, message, 500);
//     }
};